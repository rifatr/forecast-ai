# Optional when the frontend and API use different origins.
# Leave unset when the production server hosts the built frontend and API together.
VITE_API_URL=http://localhost:3001

# Browser-restricted Google Maps JavaScript API key with Places API enabled.
VITE_GOOGLE_MAPS_API_KEY=
